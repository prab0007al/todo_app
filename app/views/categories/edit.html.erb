<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card">
        <div class="card-header">
          <h3>Edit List</h3>
        </div>
        <div class="card-body">
          <%= form_for @category do |f| %>
            <% if @category.errors.any? %>
              <div class="alert alert-danger">
                <h5><%= pluralize(@category.errors.count, "error") %> prevented this list from being saved:</h5>
                <ul>
                  <% @category.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="form-group">
              <%= f.label :title, "List Name" %>
              <%= f.text_field :title, class: "form-control", required: true %>
            </div>

            <div class="form-group">
              <%= f.submit "Update List", class: "btn btn-primary" %>
              <%= link_to "Cancel", category_path(@category), class: "btn btn-secondary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
